[package]
name = "tauri-plugin-dns"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-dns"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }

[dependencies]
tauri = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
